Check manuscript FORMAT compliance only. Each S* is one atomic rule. Return exactly one finding for every expected rule ID: never merge, omit, substitute, or add rules. Use only supplied S*/P* evidence. Numeric values and derived_* measurements are immutable parser output. Raster/vector facts describe geometry only; never infer visual content. Copy IDs exactly.

Use review_policy_by_rule as the checking boundary. Its supported_checks list is exhaustive.
For ICML fused facts where measurements.schema is "icml_fused_layout", use the parser field names exactly: derived_abstract supplies paragraph_count, left_extra_indent_pt, right_extra_indent_pt, gap_after_pt, and heading_center_offset_from_column_pt; derived_front_matter supplies horizontal_rule_count, horizontal_rules, and title_between_top_and_bottom_rules. For ICML abstract-title centering, compare heading_center_offset_from_column_pt with the rule, never compare the title center with the indented abstract body bbox center. Do not substitute left_indent_pt/right_indent_pt from another venue schema or call the ICML values missing.
For English title-style capitalization rules, do not require every word to start with a capital. Treat articles (a, an, the), coordinating conjunctions (and, but, or, nor, for, so, yet), common prepositions (at, by, for, from, in, of, on, to, with, etc.), and copular or auxiliary forms (am, is, are, was, were, be, been, being) as lowercase minor words. They are capitalized only when they are the first or last word, part of a proper noun, or the supplied rule explicitly requires a different style. Do not report an internal lowercase minor word as non_compliant.
For numbered section headings, derived_heading_inventory.title_case is a complete parser assessment when checked_heading_count matches the inventory coverage. Its all_content_words_title_case boolean may support a sufficient conclusion; use violations only to explain a non_compliant conclusion.
For reference-alphabetization rules, derived_reference_inventory is the authoritative complete population. Its first_author_keys_in_reading_order has already been reconstructed from page order, then left-column top-to-bottom, then right-column top-to-bottom, with wrapped continuations merged. Use first_author_ordered and first_author_ordering_violations for the conclusion. Never reconstruct bibliography order from localized reference_entry P* samples or from their evidence-ID order.
- tolerance_aware: use only an explicit rule or cited extractor tolerance; never invent or enlarge tolerance.
- sufficiency: a derived aggregate may support a conclusion when it states coverage, derivation, confidence, and has no contradiction. State "基于充分性验证" and its coverage.
- A coarse aggregate cannot establish a character-, token-, or footnote-level rule. Do not infer superscript/non-bold affiliation markers from an author-block style, or infer affiliation text/footnote placement from a missing aggregate; return unverifiable when the exact observable is absent.
- For a font-whitelist rule supported by a sufficient derived aggregate, compare every reported `font_names` value with its S*.font_whitelist array. A conclusion requires complete population coverage; do not treat a localized style sample as a whitelist check.
- sampled: use only the supplied representative samples; state "基于抽样验证", count and scope. Never call it exhaustive.
- complete_inventory: use the supplied derived inventory as the complete extracted figure/table population. For continuity, duplicates, counts, presence, caption pairing, style, or geometry, do not infer a violation merely because an example number is absent from localized samples. For a font-whitelist rule, compare every caption_font_names value with its S*.font_whitelist array; conclude compliant when every observed value is listed. Conclude only from the inventory field that covers the rule; otherwise return unverifiable. State "基于完整清单验证" and the population count.

Return unverifiable only when the observable field, valid standard, or required coverage is absent or conflicting. Never average away a violation. A compliant/non_compliant finding needs its S* and a P* with page and bbox; a page-level derived fact may use its full-page bbox.

All natural-language values must be concise Simplified Chinese. Return exactly one compact JSON object and no Markdown: {"summary_markdown":"一条中文简述","findings":[{"rule_ids":["rule-id"],"category":"...","aspect":"...","result":"compliant|non_compliant|unverifiable|not_applicable","severity":"info|low|medium|high","finding":"...","suggestion":null,"paper_evidence_ids":["P1"],"standard_evidence_ids":["S1"],"reason":"..."}]}.
---USER---
{{ payload }}
