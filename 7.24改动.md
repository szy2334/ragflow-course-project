# 2026-07-24 改动说明

本次改动围绕论文理解 Agent 的对话可靠性和前端使用体验：区分论文相关问题与题外对话、强化论文回答的证据约束和失败处理、补齐等待与执行进度展示，并增加 Windows 一键启停脚本。

本说明对应提交 `4c3b07b Improve paper QA routing and chat resilience` 及本文件所在提交。`backend/.env`、数据库、日志和 API Key 均为本地忽略文件，未提交到仓库。

## 主要功能改动

### 1. 论文问题与题外对话分流

- 由意图路由模型判断问题是否与选中论文有关。
- `general_chat` 题外对话直接调用模型回答，不检索论文、不生成论文证据引用。
- 论文相关的 `fact`、`explain` 和追问继续检索论文证据，并只允许使用检索到的论文内容作事实依据。
- 新增通用对话提示词；论文回答提示词明确禁止在面向用户的文本中泄露内部证据 UUID。

涉及文件：

- `backend/app/ai/agents/intent_router.py`
- `backend/app/ai/agents/answer_generator.py`
- `backend/app/ai/graph/qa_builder.py`
- `backend/app/ai/prompts/intent_router/v1.jinja2`
- `backend/app/ai/prompts/answer_generator/v1.jinja2`
- `backend/app/ai/prompts/general_answer/v1.jinja2`（新增）
- `backend/app/ai/schemas/evidence.py`
- `backend/app/db/models.py`
- `backend/migrations/versions/20260724_0009_add_general_chat_route.py`（新增）

### 2. 回答生成、引用和失败处理

- 放开模型请求中的硬编码输出 token 上限；是否限制输出由本地 `.env` 配置决定。
- 对可重试的模型传输错误、语义修复和回答失败增加区分，保留有效草稿并按有限次数继续修复。
- 修正证据 UUID 中的数字被数字校验器误认为答案正文数字的问题。
- 强化通用对话和论文回答的回答能力、引用、数字和结构化输出校验。
- 执行记录保留模型调用耗时、输入输出 token、模型版本和错误原因，便于区分网关故障、结构化输出错误和证据校验错误。

涉及文件：

- `backend/.env.example`
- `backend/app/ai/llm/client.py`
- `backend/app/ai/graph/master_controller.py`
- `backend/app/ai/schemas/config.py`
- `backend/app/ai/validators/answerability.py`
- `backend/app/ai/validators/numeric.py`
- `backend/app/ai/validators/pipeline.py`
- `backend/app/core/config.py`
- `backend/app/runtime/executor.py`

### 3. 对话页面状态与交互

- 新增模型回答等待动画和协作执行进度动画。
- 页面刷新后恢复 `pending`、`running` 消息及其 SSE 执行过程，避免回答和进度丢失。
- 会话删除按钮保持可见，不再依赖悬停显示。
- 所有前端时间统一按 `Asia/Shanghai`（东八区）格式化。
- 执行记录和回答上下文显示支持新的通用对话路由与错误状态。

涉及文件：

- `frontend/src/api/contracts.ts`
- `frontend/src/components/AnswerContext.vue`
- `frontend/src/components/WorkflowTimeline.vue`
- `frontend/src/pages/ChatPage.vue`
- `frontend/src/pages/PaperDetailPage.vue`
- `frontend/src/pages/PapersPage.vue`
- `frontend/src/pages/ReportDetailPage.vue`
- `frontend/src/pages/ReportsPage.vue`
- `frontend/src/stores/workspace.ts`
- `frontend/src/styles/main.css`
- `frontend/src/utils/dateTime.ts`（新增）

### 4. Windows 本地运行脚本

- 提供后端依赖安装、后端启动和前端启动脚本。
- 提供仓库根目录的一键启动与一键关闭脚本。
- 启动脚本会检查依赖与端口，等待前后端健康检查；关闭脚本只处理匹配本项目 Uvicorn/Vite 特征的进程，避免仅凭端口关闭无关程序。

涉及文件：

- `backend/install_deps.cmd`（新增）
- `backend/start_backend.cmd`（新增）
- `backend/start_frontend.cmd`（新增）
- `start-all.cmd`（新增）
- `stop-all.cmd`（新增）

## 测试与回归覆盖

新增或更新了意图提示词、通用对话路由、模型客户端重试、引用与数字校验、工作流失败恢复的测试。

涉及文件：

- `backend/tests/test_intent_prompt.py`（新增）
- `backend/tests/test_llm_client.py`
- `backend/tests/test_routing.py`
- `backend/tests/test_validators.py`
- `backend/tests/test_workflow.py`

验证结果：

- 后端：`95 passed, 1 skipped`
- 前端：`npm run build` 通过

## 合并注意事项

- 合并后执行数据库迁移 `20260724_0009_add_general_chat_route.py`。
- 运行环境需要在 `backend/.env` 配置可用的 `LLM_BASE_URL`、`LLM_API_KEY` 和 `LLM_MODEL`；这些本地配置不会随分支合并。
- `start-all.cmd`、`stop-all.cmd` 适用于 Windows 本地开发环境，生产部署仍应使用现有部署流程。
